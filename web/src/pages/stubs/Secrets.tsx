import { ComingSoon } from '@/components/ComingSoon';

export default function Secrets() {
  return (
    <ComingSoon
      title="Secrets"
      icon="key-round"
      description="Encrypted vault for passwords, API keys and 2FA codes. Coming soon."
    />
  );
}
