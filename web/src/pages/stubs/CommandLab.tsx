import { ComingSoon } from '@/components/ComingSoon';

export default function CommandLab() {
  return (
    <ComingSoon
      title="Command Lab"
      icon="terminal"
      description="Build Linux, PowerShell, cmd, and Docker commands with a visual flag editor. Coming soon."
    />
  );
}
